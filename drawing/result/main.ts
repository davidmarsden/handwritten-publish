type DrawingResult={displayName:string;status:'waiting-for-elijah'|'judged';score:number|null;grade:string|null;comment:string|null;judgedAt:string|null;challenge:{id:string;title:string;imageUrl:string;elijahScore:number|null;elijahGrade:string|null};imageUrl:string};
const $=<T extends HTMLElement>(s:string)=>document.querySelector<T>(s)!;const headline=$('#headline'),intro=$('#intro'),content=$('#content'),challengeTitle=$('#challenge-title'),challengeImage=$('#challenge-image') as HTMLImageElement,challengeSelfGrade=$('#challenge-self-grade'),entryCaption=$('#entry-caption'),entryImage=$('#entry-image') as HTMLImageElement,judgement=$('#judgement'),score=$('#score'),grade=$('#grade'),comment=$('#comment');
async function loadResult(){const token=new URL(location.href).searchParams.get('token')?.trim()??'';if(!token)throw new Error('This private result link is missing its token.');const response=await fetch(`/api/drawing-result?token=${encodeURIComponent(token)}`,{headers:{accept:'application/json'}});const payload=await response.json() as {error?:string;result?:DrawingResult};if(!response.ok||!payload.result)throw new Error(payload.error||'This result could not be found.');const result=payload.result;content.hidden=false;challengeTitle.textContent=result.challenge.title;challengeImage.src=result.challenge.imageUrl;challengeImage.alt=`Elijah's challenge drawing: ${result.challenge.title}`;const hasSelf=result.challenge.elijahScore!==null||!!result.challenge.elijahGrade;challengeSelfGrade.textContent=hasSelf?`Elijah: ${result.challenge.elijahScore!==null?`${result.challenge.elijahScore}/10`:''}${result.challenge.elijahScore!==null&&result.challenge.elijahGrade?' — ':''}${result.challenge.elijahGrade??''}`:'';challengeSelfGrade.hidden=!hasSelf;entryCaption.textContent=`${result.displayName}'s drawing`;entryImage.src=result.imageUrl;entryImage.alt=`Drawing submitted by ${result.displayName}`;if(result.status==='judged'&&result.score!==null&&result.grade){headline.textContent='Elijah has judged it!';intro.textContent=`${result.displayName}, here's your Drawing Hand result.`;score.textContent=`${result.score}/10`;grade.textContent=result.grade;judgement.hidden=false;if(result.comment){comment.textContent=`“${result.comment}” — Elijah`;comment.hidden=false}}else{headline.textContent='Waiting for Elijah.';intro.textContent='Your drawing is safely in the Judging Desk. Keep this private link and come back after Elijah has graded it.';judgement.hidden=true}}
loadResult().catch(e=>{headline.textContent='Result not found.';intro.textContent=e instanceof Error?e.message:'This result could not be loaded.';content.hidden=true});
